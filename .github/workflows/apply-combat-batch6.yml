name: Apply Combat Batch 6

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-combat-batch6.yml

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message == 'Queue sixth exact combat batch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: python scripts/apply-combat-batch6.py
      - run: npm install
      - run: npm run build
      - run: npm run test:engine
      - name: Remove one-time patch files and commit
        run: |
          rm .github/workflows/apply-combat-batch6.yml scripts/apply-combat-batch6.py
          git config user.name "Drago"
          git config user.email "dragoidc3@gmail.com"
          git add src/engine/battle-v2.ts src/engine/support.ts .github/workflows/apply-combat-batch6.yml scripts/apply-combat-batch6.py
          git commit -m "Port sixth exact combat batch"
          git push
