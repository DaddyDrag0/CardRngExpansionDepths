name: Apply Selectable Skill Auras

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-team-auras.yml

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message == 'Queue selectable skill aura patch'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: python -m py_compile scripts/apply-team-auras.py
      - run: python scripts/apply-team-auras.py
      - run: npm install
      - run: npm run build
      - run: npm run test:engine
      - run: npm run test:depths
      - run: npm run test:player
      - name: Remove one-time patch files and commit
        run: |
          rm .github/workflows/apply-team-auras.yml scripts/apply-team-auras.py
          git config user.name "Drago"
          git config user.email "dragoidc3@gmail.com"
          git add -A
          git commit -m "Implement selectable skill auras"
          git push
