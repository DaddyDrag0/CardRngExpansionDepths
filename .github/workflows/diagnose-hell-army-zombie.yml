name: Diagnose Hell Army Zombie Dragon

on:
  push:
    paths:
      - 'scripts/diagnose-hell-army-zombie.ts'

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: npx --no-install tsx scripts/diagnose-hell-army-zombie.ts
