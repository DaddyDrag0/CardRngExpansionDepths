name: Diagnose source timeout behavior

on:
  push:
    paths:
      - 'scripts/diagnose-source-timeout.mjs'

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-audit --no-fund
      - run: node scripts/diagnose-source-timeout.mjs
      - run: npx --no-install tsx scripts/diagnose-source-timeout.ts
