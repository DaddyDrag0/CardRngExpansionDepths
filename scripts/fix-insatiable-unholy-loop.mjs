import fs from 'node:fs'

function replaceOnce(path, before, after, label) {
  let text = fs.readFileSync(path, 'utf8')
  const count = text.split(before).length - 1
  if (count !== 1) throw new Error(`${label}: expected 1 match, found ${count}`)
  text = text.replace(before, after)
  fs.writeFileSync(path, text)
}

replaceOnce(
  'src/engine/battle-v2.ts',
  `    case 'Insatiable':\n      if (target.hp <= 0 && target !== attacker) {\n        attacker.damage += target.damage * 0.3\n        attacker.maxHp += target.maxHp * 0.3\n        attacker.hp += target.maxHp * 0.3\n        attacker.flags.insatiableAttack = true\n      }\n      break`,
  `    case 'Insatiable': {\n      // Insatiable only chains after an actual defeat. Reaching 0 HP is not a\n      // defeat while a lethal-prevention ability is still keeping the card alive.\n      // Without this guard, Wendigo can repeatedly \"kill\" an Unholy Creature at\n      // 1 HP inside the same turn, so the global Unholy timer never advances.\n      const unholySurvives = hasAbility(runtime, target, 'Unholy Creature')\n        && (!target.flags.unholyActive || (target.counters.unholyTurns || 0) > 0)\n      const undyingSurvives = hasAbility(runtime, target, 'Undying')\n        && (!target.flags.undyingActive || (target.counters.undyingTurns || 0) > 0)\n      const paradoxSurvives = hasAbility(runtime, target, 'Paradox') && !target.flags.paradox\n      if (target.hp <= 0 && target !== attacker && !unholySurvives && !undyingSurvives && !paradoxSurvives) {\n        attacker.damage += target.damage * 0.3\n        attacker.maxHp += target.maxHp * 0.3\n        attacker.hp += target.maxHp * 0.3\n        attacker.flags.insatiableAttack = true\n      }\n      break\n    }`,
  'Insatiable confirmed-kill guard',
)

replaceOnce(
  'src/engine/battle-v2.ts',
  `      while (attacker.flags.insatiableAttack && alive(attacker) && active(runtime, enemyTeam)) {\n        attacker.flags.insatiableAttack = false\n        dealDamage(runtime, attacker, active(runtime, enemyTeam)!)\n        resolveDeaths(runtime)\n      }`,
  `      let insatiableChainCount = 0\n      while (attacker.flags.insatiableAttack && alive(attacker) && active(runtime, enemyTeam)) {\n        // A confirmed-kill chain should be finite. Keep a hard same-turn guard so\n        // a future revive/death-prevention interaction can never freeze the worker.\n        if (++insatiableChainCount > 64) {\n          attacker.flags.insatiableAttack = false\n          if (runtime.captureDebug) pushDebugEvent(runtime, {\n            turn: runtime.state.turn, type: 'status', team: attacker.team,\n            card: effectiveCardName(attacker) || attacker.definition.name,\n            detail: 'Insatiable same-turn chain stopped at safety limit',\n            hp: attacker.hp, maxHp: attacker.maxHp, damage: attacker.damage,\n          })\n          break\n        }\n        attacker.flags.insatiableAttack = false\n        dealDamage(runtime, attacker, active(runtime, enemyTeam)!)\n        resolveDeaths(runtime)\n      }`,
  'Insatiable same-turn safety guard',
)

const regression = `import cards from '../src/data/cards'\nimport { generateDepthsTeam } from '../src/engine/depths'\nimport { simulateBattleV2 } from '../src/engine/battle-v2'\nimport type { DepthsEnemy, TeamLoadout } from '../src/types'\n\nfunction assert(condition: unknown, message: string): asserts condition { if (!condition) throw new Error(message) }\n\nconst zombie = cards.find((card) => card.name === 'Zombie Dragon')\nconst wendigo = cards.find((card) => card.name === 'Wendigo')\nassert(zombie && wendigo, 'Zombie Dragon/Wendigo cards missing')\n\n// Direct regression for the old infinite same-turn loop: Wendigo reaches lethal\n// on Zombie Dragon, but Unholy Creature means that is not yet a confirmed kill.\nconst syntheticEnemy: DepthsEnemy[] = [{ card: wendigo, power: 1e12, health: 1e12, attack: 1e9 }]\nconst synthetic = simulateBattleV2(\n  { cards: [{ cardName: 'Zombie Dragon', borders: ['Platinum', 'Crystal'] }, { cardName: 'Hades', borders: ['Crystal', 'Galaxy'] }] },\n  syntheticEnemy, 987654321, 100, true, true,\n)\nassert(synthetic.turns <= 100, 'Wendigo/Unholy regression exceeded battle turn cap')\nassert(!synthetic.debug?.events.some((e) => e.detail.includes('Insatiable same-turn chain stopped')), 'Synthetic regression still hit Insatiable safety limit')\n\n// Recreate the user-reported floor/run seed and enemy lineup.\nfunction mixSeed(runSeed: number, floor: number): number {\n  let x = (runSeed ^ Math.imul(floor, 0x9e3779b1)) >>> 0\n  x ^= x >>> 16; x = Math.imul(x, 0x85ebca6b) >>> 0\n  x ^= x >>> 13; x = Math.imul(x, 0xc2b2ae35) >>> 0\n  return (x ^ (x >>> 16)) >>> 0\n}\nconst floor = 1625\nconst runSeed = 1241481851\nconst floorSeed = mixSeed(runSeed, floor)\nconst enemies = generateDepthsTeam(floor, floorSeed)\nconst names = enemies.map((enemy) => enemy.card.name)\nassert(names.join('|') === 'Dancer|Wendigo|Sorcerer|Michael', 'Reported enemy lineup changed: ' + names.join(' | '))\nconst loadout: TeamLoadout = { cards: [\n  { cardName: 'Zombie Dragon', borders: ['Platinum', 'Crystal'] },\n  { cardName: 'Hades', borders: ['Crystal', 'Galaxy'] },\n  { cardName: 'Fuxi', borders: [] },\n  { cardName: \"Terra's Aria\", borders: ['Platinum', 'Galaxy'] },\n] }\nconst exact = simulateBattleV2(loadout, enemies, floorSeed ^ 0x51ed270b, 10_000, true, true)\nassert(exact.turns <= 10_000, 'Reported floor did not terminate')\nassert(!exact.debug?.events.some((e) => e.detail.includes('Insatiable same-turn chain stopped')), 'Reported floor required Insatiable safety limit')\nconsole.log('Insatiable/Unholy regression passed:', { syntheticTurns: synthetic.turns, exactTurns: exact.turns, winner: exact.winner, names })\n`
fs.writeFileSync('scripts/insatiable-unholy-regression.ts', regression)

console.log('Fixed Wendigo Insatiable interaction with lethal-prevention abilities.')
